# Late-sudden trajectory parameters — moved to conf/base/parameters.yml
# (shock_year, alignment_year). They are read by four pipelines
# (inputs_processing, create_late_sudden_trajectories,
# distribute_impacts_to_asset_level, earnings_model), so they belong in the
# user-facing file, not here.
