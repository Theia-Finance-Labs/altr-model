# =============================================================================
# Advanced — inputs_postproc. Leave alone unless you know why.
# User-facing knobs are in conf/base/parameters.yml.
# =============================================================================

# meaning:  collapse the asset panel to one synthetic row per company ×
#           technology before modelling
# unit:     bool
# default:  False (asset-level)
# source:   —
# status:   advanced
reduce_granularity_from_asset_to_company_level: False

# theta_capex_recovery was retired on 2026-09-06: its consumer
# scale_electricity_price has its body commented out and returns the input
# unchanged, so the key had no effect. Re-add it here and in
# inputs_processing/pipeline.py if the scaling is ever reinstated.
