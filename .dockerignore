# Mounted at runtime rather than baked into the image - and data/ in
# particular can be hundreds of MB, which otherwise gets hashed and sent to
# the Docker daemon as build context on every build.
data/
workspace/

.git/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
